import streamlit as st

# --- نظام المصادقة بالبريد الإلكتروني للعملاء المشتركين ---
# ضع هنا إيميلات الأشخاص الذين اشتروا المنصة أو سُمح لهم بالدخول
ALLOWED_EMAILS = [
    "zakaria@example.com", 
    "friend@example.com"  # استبدل هذا بإيميل صديقك الفعلي
]

st.sidebar.title("🔐 بوابة العملاء المشتركين")
user_email = st.sidebar.text_input("أدخل بريدك الإلكتروني المسجل:")

# التحقق مما إذا كان الإيميل المدخل موجوداً في القائمة المسموحة
if user_email in ALLOWED_EMAILS:
    st.sidebar.success("✅ تم التحقق من الاشتراك بنجاح!")
    
    # ---------------------------------------------------------
    # ضع هنا كامل كود مشروعك الأصلي (لوحة التحكم، رفع الملفات، والذكاء الاصطناعي)
    # ---------------------------------------------------------
    st.title("🛡️ لوحة تحكم اكتشاف الثغرات الأمنية (SaaS)")
    st.write("أهلاً بك في النسخة الاحترافية للمنصة.")
    
    # مثال لرفع الملفات والتحليل الذي قمت ببرمجته سابقاً:
    uploaded_file = st.file_uploader("اختر ملف السجلات (CSV) للفحص", type=["csv"])
    if uploaded_file is not None:
        st.write("جاري تحليل البيانات واكتشاف التهديدات...")
        # (باقي كود التحليل والـ IsolationForest الخاص بك هنا)

else:
    # واجهة الحظر في حال لم يدخل الإيميل أو كان خطأً
    if user_email:
        st.sidebar.error("❌ هذا البريد غير مسجل أو لم يقم بالشراء.")
    
    st.warning("⚠️ يرجى إدخال بريدك الإلكتروني الصحيح في الشريط الجانبي للوصول إلى المنصة وتحليل البيانات.")
    st.stop("يرجى إتمام تسجيل الدخول عبر البريد الإلكتروني للمتابعة.")
  
